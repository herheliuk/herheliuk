# 💜 Hello there, Digital Artist

## ⚙️ My Tech Stack

- **Caddy** *(goodbye, nginx 👋)*
- **Bun** *(so long Node.js)*
- **Flask + SeaSurf** *(keep it simple)*
- **SQLite** *(too many connections)*
- **Hono + EJS** *(if you don't do python)*
- **Vite + TailwindCSS** *(React, simplified)*

### 🧠 Languages
- **Python** 🐍 (Because I write code that even humans can read)
- **TypeScript** 🔷 (Proving Oracle wrong, one line at a time)
- **SQL** 🗂️ (just don't let me write quieries for prod...)

---

## 🛠️ Essentials that Match My Vibe

- **macOS** *(Windows, you're not invited)*
- **ChatGPT** *(hey, that’s me!)* @ [chat.com](https://chat.com)

---

`nano /etc/zshrc`
- `alias npm='bun'`
- `alias npx='bunx'`

git

- `git config --global alias.pushh '!sh -c "git add . && git commit -m '\''pushed'\'' && git push"'`
- `git config --global alias.pushf '!sh -c "git add . && git commit -m '\''forced'\'' || true && git push -f"'`
- `git config --global alias.head '!sh -c "git reset --hard HEAD"'`
- `git config --global alias.wipe '!sh -c "set -e && git checkout --orphan git-wipe && git add -A && git commit -m '\''wiped'\'' && git branch -D main && git branch -m main && git push -f origin main"'`
